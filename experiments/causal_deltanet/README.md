# Causal DeltaNet Experiments

This idea family explores replacing full causal attention with causal DeltaNet-style sequence mixers.

Current runs:

- `dual_causal_ttt/`: two parallel causal delta branches, one for content update and one for multiplicative gating
